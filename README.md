# SZR Outbreak Outcome Predictor
### CSCI/DASC AI Topics — Project 2
**Author:** Curtis Moore | **Institution:** East Carolina University

---

## Overview

A surrogate neural network that predicts zombie outbreak outcomes for North Carolina counties using real census-derived Human Survivability Index (HSI) data. Instead of running a slow SZR epidemiological simulation at inference time, the trained MLP approximates the simulation output in milliseconds.

The project satisfies the first option of the assignment — **training an AI from scratch on a unique dataset** — using synthetic data generated by running thousands of SZR simulations with parameters anchored to real NC census data.

**Live app:** Deployed on GCP Cloud Run via Docker + GitHub Actions CI/CD

**Repo:** https://github.com/ckmoore2/Project-2-SZR-Outbreak-Outcome-Predictor

---

## What It Does

Given eight input features describing an NC county and outbreak scenario, the model simultaneously predicts:
- **Peak zombie fraction** — maximum fraction of the population that becomes infected at any point
- **Time to peak** — day on which the zombie population peaks
- **Containment probability** — binary classifier: does the susceptible population stabilise above 10%?

The Streamlit app provides interactive controls for county selection, zombie show scenario presets, outbreak severity levels, and fine-tune parameter sliders.

---

## Architecture

```
SZRPredictor (Experiment D — best model)
  Input (8 features)
  → Linear(8 → 128)   → ReLU → Dropout(0.2)
  → Linear(128 → 256) → ReLU → Dropout(0.2)
  → Linear(256 → 128) → ReLU → Dropout(0.2)
  → Linear(128 → 3)
     ├── outputs[0:2] → peak_zombie_fraction, time_to_peak  (MSE loss)
     └── outputs[2]   → containment logit                   (BCEWithLogitsLoss, pos_weight=5.5)
```

---

## The HSI (Human Survivability Index)

The HSI is a composite score computed from six categories of real NC census data, weighted as follows:

| Category | Weight | Data Sources |
|---|---|---|
| H — Health & Fitness | 0.15 | CDC PLACES 2023 (obesity ×0.30, LPA inactivity ×0.20), ACS B18105 (ambulatory disability ×0.30), nursing home density proxy ×0.20 |
| M — Mobility & Escape | 0.15 | ACS B25044 (vehicle access), USDA RUCA rural codes, road density |
| I — Infrastructure | 0.20 | RUCA proxy for self-sufficiency, grid independence, food access |
| E — Education & Awareness | 0.10 | ACS S1501 attainment ×0.45+0.30, FCC ULS ham radio density ×0.15, ACS C24010 agricultural occupation ×0.10 |
| C — Social & Community | 0.25 | ACS B21001 veterans ×0.30, gun permit proxy ×0.30, USFA VFD registry ×0.15 (956 depts), harvest proxy ×0.10, military county boost ×0.15 |
| G — Geographic | 0.15 | NOAA elevation/precipitation proxy by region (mountain/piedmont/coastal) |

**HSI formula:**
```
HSI_raw = 0.15×H + 0.15×M + 0.20×I + 0.10×E + 0.25×C + 0.15×G
HSI     = (HSI_raw − min) / (max − min)   → [0, 1] across NC counties
```

**Effect on SZR parameters (v2 sigmoid modifier):**
```
κ_eff(tract) = κ_base × sigmoid_modifier(HSI)
β_eff(tract) = β_base × (1 + 0.40×density) × (1 − 0.25×mobility_score)
```

The sigmoid modifier captures threshold effects — communities above HSI=0.5 become dramatically more effective at zombie removal, not just marginally better.

---

## Dataset Generation (v2)

The synthetic training data is generated by `data/generate_data.py` which now:

1. **Loads real NC HSI distributions** from `data/processed/hsi_distributions.json`, produced by `data/integrate_real_hsi.py` from the team's merged county DataFrame.
2. **Samples HSI sub-scores** from clipped normal distributions anchored to actual NC county means and standard deviations — not wide uniform ranges.
3. **Stratifies across five operational severity profiles** (`early_detection`, `active_spread`, `rapid_outbreak`, `total_collapse`, `military`) covering β ∈ [0.05, 0.90] to ensure the model trains on the full range of outbreak dynamics. Show-canon scenarios (TWD, TLOU, etc.) are app presets only — their β values are too small to produce varied outcomes in training data.
4. **Applies mild HSI modulation** (±7.5%) to β/ζ within each severity band so high-HSI counties get slightly lower transmission and higher removal rates.

This ensures synthetic training examples are drawn from the same distribution as real NC counties, making the model's predictions more meaningful for actual county-level inputs.

---

## Ablation Experiments

Six ablation experiments were run to validate model design:

| Experiment | Description | MAE | RMSE | R² | F1 | AUC |
|---|---|---|---|---|---|---|
| A | Linear/Logistic baseline | 7.355 | 17.126 | 0.625 | 0.997 | 1.000 |
| B | MLP [32] | 6.320 | 15.941 | 0.685 | 0.952 | 0.999 |
| C | MLP [64, 128] | 1.756 | 7.434 | 0.914 | 0.980 | 0.999 |
| **D** | **MLP [128, 256, 128] ← primary model** | **0.928** | **3.981** | **0.937** | **0.967** | **0.998** |
| E | MLP [64, 128], no HSI features | 1.441 | 6.462 | 0.924 | 0.984 | 0.999 |
| F | MLP [64, 128], only HSI features | 9.878 | 21.000 | −0.002 | 0.646 | 0.509 |

**Key findings:**

- **Experiment D [128, 256, 128] is the primary model** — best MAE (0.928) and RMSE (3.981), with R²=0.937.
- **Real VFD data strengthened the HSI signal.** With the USFA volunteer fire department data integrated into social scores, Experiment D (with HSI, R²=0.937) now outperforms Experiment E (no HSI, R²=0.924) — confirming that the enriched census data contributes meaningful predictive signal when the model has sufficient capacity.
- **Architecture capacity is required to leverage HSI.** Experiment C (with HSI, [64,128], R²=0.914) is outperformed by Experiment E (no HSI, [64,128], R²=0.924), but Experiment D (with HSI, [128,256,128], R²=0.937) beats both. The HSI × epidemiological parameter interactions require a wider/deeper network to exploit.
- **Experiment F (only HSI, R²≈0) confirms** that epidemiological parameters (β, ζ, α) are the dominant predictors. HSI modulates outbreak dynamics but cannot predict outcomes in isolation.

---

## Model Improvements (v1 → v2)

**1. Sigmoid κ modifier** (was linear)
- v1: `κ_eff = κ_base × (1 + 0.5 × HSI)` — proportional, range [1.0, 1.5]×κ_base
- v2: `κ_eff = κ_base × sigmoid_modifier(HSI)` — captures coordination threshold at HSI=0.5

**2. Tract-level β variation** (was uniform)
- v1: β constant everywhere regardless of density or mobility
- v2: `β_eff = β_base × (1 + 0.40×density) × (1 − 0.25×mobility)` — gives M section direct influence on spread rate

**3. Real HSI distribution anchoring** (was random)
- v1: HSI sub-scores sampled from uniform [0, 1]
- v2: Sampled from clipped normals anchored to real NC county distributions

---

## SZR Model Note

The app uses a **frequency-dependent** SZR formulation:
```
dS/dt = −β × S × Z / N
dZ/dt =  β × S × Z / N − (κ + α) × Z
dR/dt =  (κ + α) × Z
```

The simulation notebook (`Model_Simulation_1.ipynb`) uses a **mass-action** formulation without the `/N` denominator. The frequency-dependent form is more standard for human diseases and scales correctly across county populations of different sizes.

---

## Project Structure

```
Project-2-SZR-Outbreak-Outcome-Predictor/
│
├── app/
│   └── streamlit_app.py              ← Interactive Streamlit prediction UI
│
├── data/
│   ├── rebuild_county_data.py        ← Rebuilds all county CSVs from public APIs
│   ├── integrate_real_hsi.py         ← Computes per-county HSI + hsi_distributions.json
│   ├── generate_data.py              ← Generates 10k synthetic SZR training scenarios
│   ├── utils.py                      ← Shared normalize() helper
│   ├── nc_county_profiles.py         ← County profile definitions and metadata
│   ├── synthesize_missing_hsi.py     ← Fills missing HSI sub-scores via regional median
│   ├── validate_hsi_sections.py      ← Validates HSI section outputs before merging
│   ├── raw/
│   │   ├── cdc_places_inactivity.csv ← CDC PLACES LPA (tract-level, 100 counties)
│   │   ├── acs_agriculture_occupation.csv ← ACS C24010 ag workers by county
│   │   ├── nc_vfd_coverage.csv       ← USFA fire department census (1,089 NC depts)
│   │   ├── zcta_to_county.csv        ← ZCTA5→county crosswalk for ham radio mapping
│   │   └── fcc_ham/
│   │       └── nc_ham_licenses_active.csv ← FCC ULS active amateur licenses (43k)
│   ├── fetch/
│   │   ├── MANUAL_FETCH_INSTRUCTIONS.md ← Steps for VFD (done) and congregation data
│   │   ├── fetch_category_c_extensions.py ← Category C fetch scripts
│   │   ├── fetch_category_e_extensions.py ← Category E/H fetch scripts
│   │   └── fetch_category_g_extensions.py ← Category G fetch scripts
│   └── processed/                    ← All gitignored; regenerated by rebuild scripts
│       ├── merged_county_df.csv
│       ├── nc_county_hsi_real.csv
│       └── hsi_distributions.json
│
├── model/
│   ├── szr_predictor.py              ← SZRPredictor MLP class + FEATURE_COLUMNS
│   ├── train.py                      ← Six ablation experiments; saves Exp D as best
│   ├── config.py                     ← Scenario parameters, HSI weights, v2 settings
│   ├── szr_model.py                  ← SZR ODE with sigmoid κ modifier + tract-level β
│   └── sensitivity_analysis.py      ← HSI weight sensitivity sweep
│
├── notebooks/
│   ├── Model Simulation 1.ipynb     ← Team simulation notebook (county-level SZR)
│   └── exploration.ipynb            ← Exploratory data analysis
│
├── outputs/                          ← All gitignored except ablation_results.csv
│   ├── best_model.pt                ← Trained Experiment D weights
│   ├── scaler.pkl                   ← Fitted StandardScaler
│   ├── ablation_results.csv         ← Six-experiment comparison table
│   ├── loss_curve.png               ← Experiment D training/validation loss
│   └── generate_plots.py            ← Script to regenerate output charts
│
├── .github/workflows/
│   ├── deploy.yml                   ← GitHub Actions CI/CD → GCP Cloud Run
│   └── ci.yml                       ← Continuous integration checks
│
├── CLAUDE.md                        ← Project notes for Claude Code sessions
├── deploy.sh                        ← Manual GCP Cloud Run deploy helper
├── Dockerfile
├── requirements.txt
└── README.md
```

---

## Setup & Run

### Prerequisites
```bash
pip install -r requirements.txt
```

### Generate data and train
```bash
# Step 1 — Rebuild all county CSVs from public APIs (Census, CDC, FCC, USFA)
python data/rebuild_county_data.py

# Step 2 — Extract real NC HSI distributions from merged county data
python data/integrate_real_hsi.py

# Step 3 — Generate 10,000 synthetic training scenarios
python data/generate_data.py

# Step 4 — Train all six ablation experiments, save best model (Exp D)
python model/train.py
```

### Run the app locally
```bash
streamlit run app/streamlit_app.py
```

### Deploy to GCP Cloud Run
Push to `main` branch — GitHub Actions builds the Docker image and deploys automatically.

> **Mac Apple Silicon note:** Docker builds targeting Cloud Run must use `--platform linux/amd64`

---

## Requirements

```
numpy
pandas
scipy
scikit-learn
torch>=2.0.0
matplotlib
seaborn
streamlit>=1.30.0
joblib
shap
imbalanced-learn
jupyter
openpyxl
requests
```

---

## Citation

Alemi, A.A., Bierbaum, M., Myers, C.R., & Sethna, J.P. (2015).
*You can run, you can hide: The epidemiology and statistical mechanics of zombie outbreaks.*
Physical Review E, 92(5), 052801.
